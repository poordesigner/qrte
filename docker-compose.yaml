services:
  app:
    image: qrte:main
    build:
      context: .
      dockerfile: docker/php/Dockerfile
    restart: unless-stopped
    env_file:
      - .env
    environment:
      DB_CONNECTION: mysql
      DB_HOST: mysql
      REDIS_HOST: redis
      REDIS_CLIENT: phpredis
      CACHE_STORE: redis
      QUEUE_CONNECTION: redis
      SESSION_DRIVER: file
      SESSION_SECURE_COOKIE: "true"
      SESSION_SAME_SITE: lax
      VIEW_COMPILED_PATH: /var/www/html/storage/framework/views
    volumes:
      - storage:/var/www/html/storage
      - public_assets:/var/www/html/public
    depends_on:
      - mysql
      - redis

  nginx:
    build:
      context: .
      dockerfile: docker/nginx/Dockerfile
    restart: unless-stopped
    expose:
      - "80"
    volumes:
      - storage:/var/www/html/storage:ro
      - public_assets:/var/www/html/public:ro
    depends_on:
      - app

  queue:
    image: qrte:main
    restart: unless-stopped
    command: sh -c "php artisan queue:work --sleep=3 --tries=3 --timeout=120 || sleep 3600"
    env_file:
      - .env
    environment:
      DB_CONNECTION: mysql
      DB_HOST: mysql
      REDIS_HOST: redis
      REDIS_CLIENT: phpredis
      CACHE_STORE: redis
      QUEUE_CONNECTION: redis
      SESSION_DRIVER: file
      SESSION_SECURE_COOKIE: "true"
      SESSION_SAME_SITE: lax
      VIEW_COMPILED_PATH: /var/www/html/storage/framework/views
    volumes:
      - storage:/var/www/html/storage
    depends_on:
      - mysql
      - redis

  scheduler:
    image: qrte:main
    restart: unless-stopped
    command: sh -c "php artisan schedule:work || sleep 3600"
    env_file:
      - .env
    environment:
      DB_CONNECTION: mysql
      DB_HOST: mysql
      REDIS_HOST: redis
      REDIS_CLIENT: phpredis
      CACHE_STORE: redis
      QUEUE_CONNECTION: redis
      SESSION_DRIVER: file
      SESSION_SECURE_COOKIE: "true"
      SESSION_SAME_SITE: lax
      VIEW_COMPILED_PATH: /var/www/html/storage/framework/views
    volumes:
      - storage:/var/www/html/storage
    depends_on:
      - mysql
      - redis

  mysql:
    image: mysql:8.4
    restart: unless-stopped
    environment:
      MYSQL_DATABASE: ${DB_DATABASE:-qrte}
      MYSQL_USER: ${DB_USERNAME:-qrte}
      MYSQL_PASSWORD: ${DB_PASSWORD:-change_me}
      MYSQL_ROOT_PASSWORD: ${DB_ROOT_PASSWORD:-change_root_me}
    volumes:
      - mysql_data:/var/lib/mysql

  redis:
    image: redis:7-alpine
    restart: unless-stopped
    volumes:
      - redis_data:/data

volumes:
  mysql_data:
  redis_data:
  storage:
  public_assets:

